import React from 'react';
import TodoListApp from './TodoListApp';

/* TODO: The user can filter the todos by typing into an input text in the header. */
/* TODO: The filter will search through the todo names. */
/* TODO: 
    By pressing 'Enter' when the todo filter is focused, a new TodoItem must be inserted with a title that matches the filter. 
    After a new item has been added, the filter should be cleared. 
*/
/* TODO: The user can toggle a single todo by clicking on its corresponding list item. */
/* TODO: A section in the footer will display the number of completed and remaining todos. */


export default function App()  {
    return <TodoListApp />
}
